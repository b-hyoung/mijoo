# Infra Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Docker Compose로 frontend / backend / scheduler 3개 컨테이너를 한 번에 실행

**Architecture:** docker-compose.yml 하나로 전체 스택 관리. backend와 frontend는 각자 Dockerfile 빌드. scheduler는 backend 이미지 재사용해서 APScheduler만 단독 실행.

**Tech Stack:** Docker, Docker Compose v2

---

## File Structure

```
mijoo/
├── docker-compose.yml
├── .env.example
└── .gitignore
```

---

## Task 1: docker-compose.yml

**Files:**
- Create: `docker-compose.yml`
- Create: `.env.example`
- Create: `.gitignore`

- [ ] **Step 1: .env.example 작성**

```
OPENAI_API_KEY=sk-...
NEWS_API_KEY=your_newsapi_key
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
DISCORD_ALERT_THRESHOLD=5.0
```

- [ ] **Step 2: .gitignore 작성**

```
.env
*.db
backend/app/ml/saved/
__pycache__/
.next/
node_modules/
.superpowers/
```

- [ ] **Step 3: docker-compose.yml 작성**

```yaml
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  scheduler:
    build: ./backend
    command: python -c "
      from app.database import init_db;
      from app.scheduler import start_scheduler;
      import time;
      init_db();
      start_scheduler();
      print('Scheduler running...');
      [time.sleep(60) or None for _ in iter(int, 1)]
    "
    env_file: .env
    volumes:
      - ./data:/app/data
    depends_on:
      backend:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
```

- [ ] **Step 4: .env 파일 생성 (실제 키 입력)**

```bash
cp .env.example .env
# .env 파일 열어서 실제 API 키 입력
```

- [ ] **Step 5: 전체 실행 확인**

```bash
docker-compose up --build
```
Expected:
- `http://localhost:3000` → 대시보드 접속
- `http://localhost:8000/health` → `{"status": "ok"}`
- `http://localhost:8000/docs` → FastAPI Swagger UI

- [ ] **Step 6: Commit**

```bash
git add docker-compose.yml .env.example .gitignore
git commit -m "chore: docker compose full stack setup"
```

---

## 실행 명령어 요약

```bash
# 전체 시작
docker-compose up --build

# 백그라운드 실행
docker-compose up -d

# 로그 확인
docker-compose logs -f backend

# 종료
docker-compose down
```
